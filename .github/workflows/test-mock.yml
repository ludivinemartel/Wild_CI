name: 1 - test mock front

on:
  push:
    branches: ["1-mock-livres"]

jobs:
  tests:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: 📌 Checkout Repo
        uses: actions/checkout@v3